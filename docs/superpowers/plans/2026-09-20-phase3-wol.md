# Phase 3：WOL 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Power Rail 上的 "Wake PC" 真的把关机状态的 PC 唤醒：构造标准 WOL 魔包、对广播地址发 3 次、进入 `WAKING` 后每 2 秒轮询 `GET /api/v1/status`，Agent 应答即 `ONLINE`，超时回落 `WOL_READY` 并给出可读原因。

**Architecture:** 魔包是纯函数（commonMain，可单测）；UDP 发送是平台能力（`WakeOnLanSender` 接口 + androidMain 的 `DatagramSocket` 实现，与 Phase 2 的 `TcpProbe`/`KeyValueStore` 同一套"接口在 common、实现在 android、入口注入"的骨架）；唤醒编排 `WakeSequence` 是 commonMain 的挂起函数，sender 与状态探测都以参数注入，因此 60 秒等待能在 `runTest` 的虚拟时间里秒级验证。App 只负责把 `RailEvent.Primary` 接到 `WakeSequence` 并把 `PcEvent` 喂给既有 `PcStateMachine`。

**Tech Stack:** Kotlin 2.2.10 / Compose Multiplatform 1.10.3 / kotlinx-coroutines 1.11.0 / kotlinx-datetime 0.8.0 / 既有 Ktor `AgentApi`（HTTP 轮询）。**不新增任何依赖**：UDP 用 JDK `java.net.DatagramSocket`（androidMain）。

**Spec:** [docs/superpowers/specs/2026-09-19-desktop-companion-design.md](../specs/2026-09-19-desktop-companion-design.md) §5（WOL = UDP 广播端口 9、发 3 次、随后每 2 秒轮询 `GET /api/v1/status`）、§4 状态机（`WOL_READY → WAKING → ONLINE/OFFLINE`）、§7.5 Device Setup
**路标：** [2026-09-19-roadmap.md](2026-09-19-roadmap.md) Phase 3 段
**前序计划：** [2026-09-19-phase0-kmp-foundation.md](2026-09-19-phase0-kmp-foundation.md)、[2026-09-19-phase1-design-system-and-mock-ui.md](2026-09-19-phase1-design-system-and-mock-ui.md)、[2026-09-20-phase2-device-system.md](2026-09-20-phase2-device-system.md)

---

## 0. 计划期定案

| 决策 | 选定 | 理由与代价 |
| --- | --- | --- |
| 魔包编码位置 | `core/wol/MagicPacket.kt`（commonMain 纯函数） | 路标要求"魔法包字节序列有单元测试"；纯函数才能在 `commonTest` 里逐字节断言。代价：无。 |
| UDP 发送 | `WakeOnLanSender` 接口 + androidMain 的 `java.net.DatagramSocket` | 与 `TcpProbe`/`KeyValueStore` 同构；commonMain 不能 import `java.*`。不引 `ktor-network`：UDP 广播用 JDK 一句话就够，多一个依赖不划算。代价：将来非 JVM 平台要另写实现。 |
| 发送目标 | `PcDevice.broadcastAddress : PcDevice.wolPort`，默认 `192.168.1.255`（Phase 2 模型已有） | spec §6 的模型字段就是为此准备的；`255.255.255.255` 只是 `broadcastAddress` 的一个可选值，不硬编码。代价：用户没填对广播地址就唤不醒（UI 有校验与"Wake-on-LAN requires MAC"提示）。 |
| 重试与轮询节奏 | 发送 3 次（间隔 150 ms，spec：3 次）；轮询 2 s 一次（spec），预算 60 s | 60 s 是"覆盖典型 S3→POST/BIOS 时间"的经验值，写成 `WakeSequence` 的命名常量，便于后续调。代价：预算写死在代码里，真机验收若发现不够要改常量。 |
| 超时后的状态 | 回落 `WOL_READY`（不是 `OFFLINE`） | `PcStateMachine` 已有 `WakeTimedOut → WOL_READY`，且 `WOL_READY` 的能力表允许再按一次（"Wake PC" 可点）。代价：用户看到的是"回到待唤醒"，需要一句 note 说明失败原因 —— Task 4 补。 |
| 真机 10/10 验收 | 由**用户**在有真实 PC 的局域网上执行，计划提供可复现步骤与记录表 | 我这边没有可控的真 PC（模拟器的 SLIRP 网络发不出到真实 LAN 的广播，Phase 2 的 T8 已实测 `10.0.2.2` 别名在本机不可达）。代价：路标的"连续 10 次开机成功"这条在计划执行完时仍是**待用户确认**，不会被伪造成已验证。 |

## Global Constraints

- **`commonMain` 禁止 import `android.*`**；UDP、DataStore、TCP 这类平台能力一律"接口在 common、实现在 android、入口（`MainActivity`）注入"。
- **颜色、间距、圆角、字号只能取自 `core/theme`**；Composable 内禁止字面量 `Color(0x...)` 与裸 `dp`。
- **每个 UI 元素必须有 `testTag`**，命名 `区域:元素`。
- **状态流转只能走 `PcStateMachine.reduce`**，禁止在 UI 或编排里直接赋 `PcState`。
- **失败必须可解释**：唤醒失败要能在界面上落成一句带原因的话（缺 MAC / 缺广播地址 / socket 错误 / 60 s 无应答），不许只有"失败"。
- **不新增依赖**；不引入图标库。
- **每笔提交前** `./gradlew :composeApp:testDebugUnitTest :composeApp:desktopTest` 必须全绿；动 androidMain 时加 `:composeApp:assembleDebug`。

---

## 1. 文件结构

| 文件 | 职责 |
| --- | --- |
| `core/wol/MagicPacket.kt`（新） | 6×`0xFF` + MAC×16 = 102 字节；唯一编码入口 |
| `core/wol/WakeOnLanSender.kt`（新） | `WakeOnLanSender` 接口 + `WakeSendResult` + `UnsupportedWakeOnLanSender`（桌面/测试默认实现，如实报不可用） |
| `core/wol/WakeSequence.kt`（新） | 唤醒编排：校验 → 发包 → `WAKING` → 每 2 s 轮询 → `ONLINE`/超时；纯逻辑，sender 与状态探测注入 |
| `androidMain/.../core/wol/UdpWakeOnLanSender.kt`（新） | `DatagramSocket`（`broadcast = true`）+ 3 次发送 + 异常→原因映射 |
| `app/App.kt`（改） | 注入 `WakeOnLanSender`；`RailEvent.Primary` 在可唤醒状态下启动 `WakeSequence`；把 `WakeOutcome` 落成 rail 上的一行 note |
| `ui/powerrail/PowerRail.kt`（改） | 渲染 `wakeNote`（`powerrail:status` 的补充行） |
| `androidMain/.../MainActivity.kt`（改） | 注入 `UdpWakeOnLanSender()` |

测试：

| 文件 | 覆盖 |
| --- | --- |
| `commonTest/.../core/wol/MagicPacketTest.kt`（新） | 102 字节、前 6 字节全 `0xFF`、MAC 重复 16 次、大小写/分隔符归一化后一致 |
| `commonTest/.../core/wol/WakeSequenceTest.kt`（新） | 缺 MAC/缺广播、发送失败、首轮应答成功、60 s 无应答（虚拟时间）、事件序列 |
| `androidUnitTest/.../core/wol/UdpWakeOnLanSenderTest.kt`（新） | 真 UDP：本机 `DatagramSocket` 收 102 字节并逐字节比对；3 次尝试真的发了 3 个包；异常映射 |
| `desktopTest/.../ui/powerrail/WakeFlowTest.kt`（新） | 点主按钮 → 发一次包 → rail 进入 `WAKING`（`Waiting for Agent`）；失败 note 可见 |

---

### Task 1: 魔包编码（纯函数）

**Files:**
- Create: `mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/core/wol/MagicPacket.kt`
- Test: `mobile/composeApp/src/commonTest/kotlin/com/xukunz/wakeupmywall/core/wol/MagicPacketTest.kt`

**Interfaces:**
- Consumes: `MacAddress`（`parse`/`bytes`/`normalized`）
- Produces: `object MagicPacket { const val MAC_REPEATS = 16; val SIZE: Int; fun encode(mac: MacAddress): ByteArray }`

- [x] **Step 1: 写失败测试**

```kotlin
package com.xukunz.wakeupmywall.core.wol

import com.xukunz.wakeupmywall.domain.model.MacAddress
import kotlin.test.Test
import kotlin.test.assertContentEquals
import kotlin.test.assertEquals

class MagicPacketTest {

    private val mac = MacAddress.parse("00:1A:2B:3C:4D:5E")!!

    @Test
    fun `packet is six ff bytes followed by the mac sixteen times`() {
        val packet = MagicPacket.encode(mac)

        assertEquals(102, packet.size)
        assertContentEquals(ByteArray(6) { 0xFF.toByte() }, packet.copyOfRange(0, 6))
        repeat(MagicPacket.MAC_REPEATS) { repeat ->
            val start = 6 + repeat * 6
            assertContentEquals(
                byteArrayOf(0x00, 0x1A, 0x2B, 0x3C, 0x4D, 0x5E),
                packet.copyOfRange(start, start + 6),
            )
        }
    }

    @Test
    fun `dash separated and lowercase input encodes identically`() {
        val dashed = MacAddress.parse("00-1a-2b-3c-4d-5e")!!

        assertContentEquals(MagicPacket.encode(mac), MagicPacket.encode(dashed))
    }

    @Test
    fun `the tail of the packet is the mac, not padding`() {
        val packet = MagicPacket.encode(mac)

        assertContentEquals(
            byteArrayOf(0x00, 0x1A, 0x2B, 0x3C, 0x4D, 0x5E),
            packet.copyOfRange(packet.size - 6, packet.size),
        )
    }
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `JAVA_HOME=<jdk25> ./gradlew :composeApp:desktopTest --tests "*MagicPacketTest*"`
Expected: FAIL —— `Unresolved reference 'MagicPacket'`

- [x] **Step 3: 实现**

```kotlin
package com.xukunz.wakeupmywall.core.wol

import com.xukunz.wakeupmywall.domain.model.MacAddress

/**
 * WOL 魔包（spec §5）：6 字节 `0xFF` 同步头 + 目标 MAC 重复 16 次 = 102 字节。
 * 全项目只有这一个编码入口，避免别处再手写一遍字节序列。
 */
object MagicPacket {
    const val MAC_REPEATS = 16
    const val SIZE = 6 + MAC_REPEATS * 6

    fun encode(mac: MacAddress): ByteArray {
        val packet = ByteArray(SIZE)
        for (index in 0 until 6) packet[index] = 0xFF.toByte()
        val address = mac.bytes
        repeat(MAC_REPEATS) { repeat -> address.copyInto(packet, 6 + repeat * 6) }
        return packet
    }
}
```

- [x] **Step 4: 运行测试确认通过**

Run: `JAVA_HOME=<jdk25> ./gradlew :composeApp:testDebugUnitTest :composeApp:desktopTest`
Expected: BUILD SUCCESSFUL，`MagicPacketTest` 3 条全绿

- [x] **Step 5: 提交**

```bash
git add mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/core/wol mobile/composeApp/src/commonTest/kotlin/com/xukunz/wakeupmywall/core/wol
git commit -m "feat: add the wake-on-lan magic packet encoder"
```

---

### Task 2: UDP 发送器（androidMain + 真 socket 测试）

**Files:**
- Create: `mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/core/wol/WakeOnLanSender.kt`
- Create: `mobile/composeApp/src/androidMain/kotlin/com/xukunz/wakeupmywall/core/wol/UdpWakeOnLanSender.kt`
- Test: `mobile/composeApp/src/androidUnitTest/kotlin/com/xukunz/wakeupmywall/core/wol/UdpWakeOnLanSenderTest.kt`

**Interfaces:**
- Consumes: Task 1 的 `MagicPacket`（测试里用来比对字节）
- Produces:
  - `interface WakeOnLanSender { suspend fun send(packet: ByteArray, target: String, port: Int): WakeSendResult }`
  - `sealed interface WakeSendResult { data class Sent(val packets: Int) : WakeSendResult; data class Failed(val reason: String) : WakeSendResult }`
  - `object UnsupportedWakeOnLanSender : WakeOnLanSender`
  - `class UdpWakeOnLanSender(attempts: Int = 3, gapMillis: Long = 150) : WakeOnLanSender`
  - `internal fun describeSendFailure(error: IOException): String`

- [x] **Step 1: 写失败测试**

```kotlin
package com.xukunz.wakeupmywall.core.wol

import com.xukunz.wakeupmywall.domain.model.MacAddress
import java.net.DatagramPacket
import java.net.DatagramSocket
import java.net.InetAddress
import java.net.SocketException
import java.net.UnknownHostException
import kotlinx.coroutines.test.runTest
import kotlin.test.Test
import kotlin.test.assertContentEquals
import kotlin.test.assertEquals
import kotlin.test.assertTrue

class UdpWakeOnLanSenderTest {

    private val packet = MagicPacket.encode(MacAddress.parse("00:1A:2B:3C:4D:5E")!!)

    @Test
    fun `the packet arrives byte for byte on a real socket`() = runTest {
        DatagramSocket(0, InetAddress.getByName("127.0.0.1")).use { listener ->
            listener.soTimeout = 3_000
            val result = UdpWakeOnLanSender(attempts = 1).send(packet, "127.0.0.1", listener.localPort)

            assertEquals(WakeSendResult.Sent(1), result)

            val buffer = ByteArray(256)
            val received = DatagramPacket(buffer, buffer.size)
            listener.receive(received)
            assertContentEquals(packet, received.data.copyOfRange(0, received.length))
            assertEquals(102, received.length)
        }
    }

    @Test
    fun `three attempts really send three datagrams`() = runTest {
        DatagramSocket(0, InetAddress.getByName("127.0.0.1")).use { listener ->
            listener.soTimeout = 3_000
            val result = UdpWakeOnLanSender(attempts = 3, gapMillis = 10).send(packet, "127.0.0.1", listener.localPort)

            assertEquals(WakeSendResult.Sent(3), result)

            val buffer = ByteArray(256)
            repeat(3) { index ->
                val received = DatagramPacket(buffer, buffer.size)
                listener.receive(received)
                assertEquals(102, received.length, "packet #${index + 1} should be 102 bytes")
            }
        }
    }

    @Test
    fun `failure reasons are typed, not guessed from message text`() {
        assertEquals("Cannot resolve \"nope.invalid\"", describeSendFailure(UnknownHostException("nope.invalid")))
        assertTrue(describeSendFailure(SocketException("Network is unreachable")).contains("Network is unreachable"))
    }
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `JAVA_HOME=<jdk25> ./gradlew :composeApp:testDebugUnitTest --tests "*UdpWakeOnLanSenderTest*"`
Expected: FAIL —— `Unresolved reference 'UdpWakeOnLanSender'` / `describeSendFailure`

- [x] **Step 3: 实现**

```kotlin
// commonMain/core/wol/WakeOnLanSender.kt
package com.xukunz.wakeupmywall.core.wol

sealed interface WakeSendResult {
    data class Sent(val packets: Int) : WakeSendResult
    data class Failed(val reason: String) : WakeSendResult
}

interface WakeOnLanSender {
    suspend fun send(packet: ByteArray, target: String, port: Int): WakeSendResult
}

/** 桌面预览与纯 UI 测试的默认实现：如实说这条平台路径不可用，不假装发送成功。 */
object UnsupportedWakeOnLanSender : WakeOnLanSender {
    override suspend fun send(packet: ByteArray, target: String, port: Int) =
        WakeSendResult.Failed("Wake-on-LAN is not available on this platform")
}
```

```kotlin
// androidMain/core/wol/UdpWakeOnLanSender.kt
package com.xukunz.wakeupmywall.core.wol

import java.io.IOException
import java.net.DatagramPacket
import java.net.DatagramSocket
import java.net.InetAddress
import java.net.SocketException
import java.net.UnknownHostException
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.delay
import kotlinx.coroutines.withContext

/**
 * UDP 广播发送魔包。重试 3 次（spec §5），每次之间留一点间隔，避免被同一条网卡的
 * 队列合并丢弃。失败原因按异常**类型**分类，不去猜 message 文案。
 */
class UdpWakeOnLanSender(
    private val attempts: Int = 3,
    private val gapMillis: Long = 150,
) : WakeOnLanSender {

    override suspend fun send(packet: ByteArray, target: String, port: Int): WakeSendResult =
        withContext(Dispatchers.IO) {
            try {
                DatagramSocket().use { socket ->
                    socket.broadcast = true
                    val address = InetAddress.getByName(target)
                    val datagram = DatagramPacket(packet, packet.size, address, port)
                    var sent = 0
                    repeat(attempts) { index ->
                        socket.send(datagram)
                        sent++
                        if (index < attempts - 1) delay(gapMillis)
                    }
                    // 真机验收（Task 5）靠这行 logcat 证明"包真的发出去了"：adb logcat -s WakeOnLan
                    logger("sent ${packet.size} bytes x$sent to $target:$port")
                    WakeSendResult.Sent(sent)
                }
            } catch (e: IOException) {
                WakeSendResult.Failed(describeSendFailure(e))
            }
        }
}

/** 发送失败的原因文案，单独提出来才能用合成异常单测。 */
internal fun describeSendFailure(error: IOException): String = when (error) {
    is UnknownHostException -> "Cannot resolve \"${error.message}\""
    is SocketException -> "Socket error: ${error.message ?: "broadcast rejected"}"
    else -> error.message ?: error::class.simpleName ?: "send failed"
}
```

- [x] **Step 4: 运行测试确认通过**

Run: `JAVA_HOME=<jdk25> ./gradlew :composeApp:testDebugUnitTest :composeApp:desktopTest`
Expected: BUILD SUCCESSFUL，`UdpWakeOnLanSenderTest` 3 条全绿

- [x] **Step 5: 提交**

```bash
git add mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/core/wol mobile/composeApp/src/androidMain/kotlin/com/xukunz/wakeupmywall/core/wol mobile/composeApp/src/androidUnitTest/kotlin/com/xukunz/wakeupmywall/core/wol
git commit -m "feat: send wake-on-lan magic packets over udp"
```

> 注意：日志必须**注入**，不能直接调 `android.util.Log` —— 执行时实测 `testDebugUnitTest` 里
> `Log.d` 会抛 `RuntimeException: Method d in android.util.Log not mocked`（android.jar 是 stub）。
> 因此构造函数收一个 `logger: (String) -> Unit = {}`，`MainActivity` 接线时传
> `{ Log.d("WakeOnLan", it) }`，单元测试走默认空实现。

---

### Task 3: 唤醒编排（WakeSequence，纯逻辑 + 虚拟时间单测）

**Files:**
- Create: `mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/core/wol/WakeSequence.kt`
- Test: `mobile/composeApp/src/commonTest/kotlin/com/xukunz/wakeupmywall/core/wol/WakeSequenceTest.kt`

**Interfaces:**
- Consumes: `WakeOnLanSender` / `WakeSendResult`（Task 2）、`MagicPacket`（Task 1）、`PcDevice`、`PcEvent`
- Produces:
  - `enum class WakeFailure { MISSING_MAC, MISSING_BROADCAST, SEND_FAILED }`
  - `sealed interface WakeOutcome { AgentOnline(packets, waitedMillis) / NoAnswer(packets, waitedMillis, lastError) / Failed(reason, message) }`
  - `class WakeSequence(sender, agentResponded: suspend (PcDevice) -> Boolean, pollIntervalMillis = 2_000, waitBudgetMillis = 60_000, onEvent: (PcEvent) -> Unit = {})` + `suspend fun wake(device: PcDevice): WakeOutcome`

- [ ] **Step 1: 写失败测试**

```kotlin
package com.xukunz.wakeupmywall.core.wol

import com.xukunz.wakeupmywall.domain.model.MacAddress
import com.xukunz.wakeupmywall.domain.model.PcDevice
import com.xukunz.wakeupmywall.domain.model.PcEvent
import kotlinx.coroutines.test.runTest
import kotlin.test.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue

private class RecordingSender(private val result: WakeSendResult) : WakeOnLanSender {
    val calls = mutableListOf<Triple<Int, String, Int>>()
    override suspend fun send(packet: ByteArray, target: String, port: Int): WakeSendResult {
        calls += Triple(packet.size, target, port)
        return result
    }
}

class WakeSequenceTest {

    private val device = PcDevice(
        id = "my-pc",
        name = "My PC",
        macAddress = MacAddress.parse("00:1A:2B:3C:4D:5E"),
        broadcastAddress = "192.168.1.255",
        wolPort = 9,
        isDefault = true,
    )

    private fun sequence(
        sender: WakeOnLanSender,
        answers: List<Boolean>,
        events: MutableList<PcEvent> = mutableListOf(),
    ) = WakeSequence(
        sender = sender,
        agentResponded = answers.let { list -> var index = 0; { _ -> list.getOrElse(index++) { false } } },
        pollIntervalMillis = 2_000,
        waitBudgetMillis = 60_000,
        onEvent = { events += it },
    )

    @Test
    fun `a device without a mac never sends`() = runTest {
        val sender = RecordingSender(WakeSendResult.Sent(3))

        val outcome = sequence(sender, emptyList()).wake(device.copy(macAddress = null))

        assertEquals(WakeFailure.MISSING_MAC, (outcome as WakeOutcome.Failed).reason)
        assertTrue(sender.calls.isEmpty())
    }

    @Test
    fun `a device without a broadcast address never sends`() = runTest {
        val sender = RecordingSender(WakeSendResult.Sent(3))

        val outcome = sequence(sender, emptyList()).wake(device.copy(broadcastAddress = "  "))

        assertEquals(WakeFailure.MISSING_BROADCAST, (outcome as WakeOutcome.Failed).reason)
        assertTrue(sender.calls.isEmpty())
    }

    @Test
    fun `a send failure is reported verbatim and never enters waking`() = runTest {
        val sender = RecordingSender(WakeSendResult.Failed("Socket error: Network is unreachable"))
        val events = mutableListOf<PcEvent>()

        val outcome = sequence(sender, emptyList(), events).wake(device)

        assertEquals(WakeFailure.SEND_FAILED, (outcome as WakeOutcome.Failed).reason)
        assertTrue(outcome.message.contains("Network is unreachable"))
        assertTrue(events.isEmpty(), "没发出去就不该进 WAKING")
    }

    @Test
    fun `the packet goes to the broadcast address and the configured port`() = runTest {
        val sender = RecordingSender(WakeSendResult.Sent(3))

        sequence(sender, listOf(true)).wake(device)

        assertEquals(Triple(102, "192.168.1.255", 9), sender.calls.single())
    }

    @Test
    fun `the first agent answer ends the wait with online`() = runTest {
        val events = mutableListOf<PcEvent>()

        val outcome = sequence(RecordingSender(WakeSendResult.Sent(3)), listOf(false, true), events).wake(device)

        assertEquals(WakeOutcome.AgentOnline(packets = 3, waitedMillis = 4_000), outcome)
        assertEquals(listOf(PcEvent.WakeRequested, PcEvent.AgentResponded), events)
    }

    @Test
    fun `no answer inside the budget ends back at wake ready`() = runTest {
        val events = mutableListOf<PcEvent>()

        val outcome = sequence(RecordingSender(WakeSendResult.Sent(3)), emptyList(), events).wake(device)

        assertEquals(WakeOutcome.NoAnswer(packets = 3, waitedMillis = 60_000, lastError = null), outcome)
        assertEquals(listOf(PcEvent.WakeRequested, PcEvent.WakeTimedOut), events)
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `JAVA_HOME=<jdk25> ./gradlew :composeApp:desktopTest --tests "*WakeSequenceTest*"`
Expected: FAIL —— `Unresolved reference 'WakeSequence'`

- [ ] **Step 3: 实现**

```kotlin
package com.xukunz.wakeupmywall.core.wol

import com.xukunz.wakeupmywall.domain.model.PcDevice
import com.xukunz.wakeupmywall.domain.model.PcEvent
import kotlinx.coroutines.delay

enum class WakeFailure { MISSING_MAC, MISSING_BROADCAST, SEND_FAILED }

sealed interface WakeOutcome {
    data class AgentOnline(val packets: Int, val waitedMillis: Long) : WakeOutcome
    data class NoAnswer(val packets: Int, val waitedMillis: Long, val lastError: String?) : WakeOutcome
    data class Failed(val reason: WakeFailure, val message: String) : WakeOutcome
}

/**
 * 唤醒编排（spec §5）：校验 → 发 3 次魔包 → 进 `WAKING` → 每 2 秒问一次 Agent
 * 是否已经起来 → 应答即 `ONLINE`，预算耗尽回落 `WOL_READY`。
 *
 * `agentResponded` 与 `sender` 都注入：这样 60 秒等待能在 `runTest` 的虚拟时间里秒级验证，
 * 而状态流转只通过 [onEvent] 发 `PcEvent`，依旧由 `PcStateMachine` 决定下一个状态。
 */
class WakeSequence(
    private val sender: WakeOnLanSender,
    private val agentResponded: suspend (PcDevice) -> Boolean,
    private val pollIntervalMillis: Long = 2_000,
    private val waitBudgetMillis: Long = 60_000,
    private val onEvent: (PcEvent) -> Unit = {},
) {
    suspend fun wake(device: PcDevice): WakeOutcome {
        val mac = device.macAddress
            ?: return WakeOutcome.Failed(WakeFailure.MISSING_MAC, "Wake-on-LAN needs the PC's MAC address")
        if (device.broadcastAddress.isBlank()) {
            return WakeOutcome.Failed(WakeFailure.MISSING_BROADCAST, "Wake-on-LAN needs a broadcast address")
        }

        return when (val result = sender.send(MagicPacket.encode(mac), device.broadcastAddress, device.wolPort)) {
            is WakeSendResult.Failed -> WakeOutcome.Failed(WakeFailure.SEND_FAILED, result.reason)
            is WakeSendResult.Sent -> {
                onEvent(PcEvent.WakeRequested)
                var waited = 0L
                while (waited < waitBudgetMillis) {
                    delay(pollIntervalMillis)
                    waited += pollIntervalMillis
                    if (agentResponded(device)) {
                        onEvent(PcEvent.AgentResponded)
                        return WakeOutcome.AgentOnline(result.packets, waited)
                    }
                }
                onEvent(PcEvent.WakeTimedOut)
                WakeOutcome.NoAnswer(result.packets, waited, lastError = null)
            }
        }
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `JAVA_HOME=<jdk25> ./gradlew :composeApp:testDebugUnitTest :composeApp:desktopTest`
Expected: BUILD SUCCESSFUL，`WakeSequenceTest` 6 条全绿（60 秒用例走虚拟时间，不会真的等）

- [ ] **Step 5: 提交**

```bash
git add mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/core/wol mobile/composeApp/src/commonTest/kotlin/com/xukunz/wakeupmywall/core/wol
git commit -m "feat: orchestrate the wake sequence with agent polling"
```

---

### Task 4: 接进 Power Rail（含失败 note）

**Files:**
- Modify: `mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/ui/powerrail/PowerRail.kt`（渲染 `wakeNote`）
- Modify: `mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/ui/powerrail/PowerRailModel.kt`（若模型不在本文件则同样修改：`wakeNote: String? = null`）
- Modify: `mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/app/App.kt`
- Modify: `mobile/composeApp/src/androidMain/kotlin/com/xukunz/wakeupmywall/MainActivity.kt`
- Test: `mobile/composeApp/src/desktopTest/kotlin/com/xukunz/wakeupmywall/ui/powerrail/WakeFlowTest.kt`

**Interfaces:**
- Consumes: `WakeSequence` / `WakeOutcome` / `WakeOnLanSender`（Task 3）、`powerRailModel(...)`
- Produces: `App(..., wakeSender: WakeOnLanSender = UnsupportedWakeOnLanSender, wakePollMillis: Long = 2_000, wakeBudgetMillis: Long = 60_000)`；rail 上新增 `powerrail:wake-note`（仅当有 note 时渲染）

- [ ] **Step 1: 写失败测试**

```kotlin
package com.xukunz.wakeupmywall.ui.powerrail

import androidx.compose.ui.test.ExperimentalTestApi
import androidx.compose.ui.test.assertTextEquals
import androidx.compose.ui.test.onNodeWithTag
import androidx.compose.ui.test.performClick
import androidx.compose.ui.test.runComposeUiTest
import com.xukunz.wakeupmywall.app.App
import com.xukunz.wakeupmywall.core.wol.WakeOnLanSender
import com.xukunz.wakeupmywall.core.wol.WakeSendResult
import com.xukunz.wakeupmywall.domain.model.PcState
import kotlinx.coroutines.runBlocking
import kotlin.test.Test
import kotlin.test.assertEquals

private class FakeSender : WakeOnLanSender {
    var sends = 0
    override suspend fun send(packet: ByteArray, target: String, port: Int): WakeSendResult {
        sends++
        return WakeSendResult.Sent(3)
    }
}

@OptIn(ExperimentalTestApi::class)
class WakeFlowTest {

    @Test
    fun `tapping the ring sends a packet and the rail shows waking`() = runComposeUiTest {
        val sender = FakeSender()
        setContent {
            App(
                initialPcState = PcState.WOL_READY,
                wakeSender = sender,
                wakePollMillis = 20,
                wakeBudgetMillis = 100,
            )
        }

        onNodeWithTag("powerrail:primary").performClick()

        assertEquals(1, sender.sends)
        // WakeRequested → WAKING：能力表的 statusText 就是 "Waiting for Agent"
        onNodeWithTag("powerrail:state", useUnmergedTree = true).assertTextEquals("Waiting for Agent")
    }

    @Test
    fun `a wake that nobody answers explains itself on the rail`() = runComposeUiTest {
        setContent {
            App(
                initialPcState = PcState.WOL_READY,
                wakeSender = FakeSender(),
                wakePollMillis = 20,
                wakeBudgetMillis = 100,
                wakeAgentResponds = { false },
            )
        }

        onNodeWithTag("powerrail:primary").performClick()
        waitUntil(timeoutMillis = 5_000) {
            onAllNodesWithTag("powerrail:wake-note").fetchSemanticsNodes().isNotEmpty()
        }

        onNodeWithTag("powerrail:wake-note", useUnmergedTree = true)
            .assertTextEquals("Sent 3 wake packets — no answer from the Agent within 1 s")
    }
}
```

> 说明：`wakeAgentResponds` 是给测试用的注入点（默认 `null` = 用真实的 `AgentApi` 探测）。文案里的 "1 s" 来自 100 ms 预算的展示值，实现时用 `waitedMillis` 换算；若四舍五入后为 `0 s`，统一显示 `<1 s`。

- [ ] **Step 2: 运行测试确认失败**

Run: `JAVA_HOME=<jdk25> ./gradlew :composeApp:desktopTest --tests "*WakeFlowTest*"`
Expected: FAIL —— `App` 没有 `wakeSender` 参数 / 找不到 `powerrail:wake-note`

- [ ] **Step 3: 实现**

```kotlin
// PowerRailModel：模型加一个可选 note
data class PowerRailModel(
    ...原有字段...,
    /** 唤醒失败/超时的解释行，null = 不显示。 */
    val wakeNote: String? = null,
)
```

```kotlin
// PowerRail.kt / PowerRailCompact.kt：状态行下方按需渲染
model.wakeNote?.let { note ->
    Text(
        text = note,
        style = MaterialTheme.typography.labelSmall,
        color = MaterialTheme.colorScheme.onSurfaceVariant,
        modifier = Modifier.testTag("powerrail:wake-note"),
    )
}
```

```kotlin
// App.kt
fun App(
    ...,
    wakeSender: WakeOnLanSender = UnsupportedWakeOnLanSender,
    wakePollMillis: Long = 2_000,
    wakeBudgetMillis: Long = 60_000,
    wakeAgentResponds: (suspend (com.xukunz.wakeupmywall.domain.model.PcDevice) -> Boolean)? = null,
) {
    var wakeNote by remember { mutableStateOf<String?>(null) }
    val wakeSequence = remember(wakeSender, wakePollMillis, wakeBudgetMillis) {
        WakeSequence(
            sender = wakeSender,
            agentResponded = wakeAgentResponds ?: { device ->
                AgentApi(createAgentHttpClient()).status("http://${device.agentHost ?: device.ipAddress}:${device.agentPort}", null)
                    is ApiResult.Success
            },
            pollIntervalMillis = wakePollMillis,
            waitBudgetMillis = wakeBudgetMillis,
            onEvent = { event -> pcState = PcStateMachine.reduce(pcState, event) },
        )
    }
    ...
    onRailEvent = { event ->
        when (event) {
            RailEvent.Settings -> navigator.goTo(Workspace.Settings)
            RailEvent.Primary -> {
                wakeNote = null
                scope.launch {
                    wakeNote = wakeSequence.wake(device).toNote()
                }
            }
            // Sleep / Shutdown / Restart 保持原样（Phase 4 接 Agent 才真正生效）
            ...
        }
    }
```

```kotlin
/** 只在"没成功"时给用户一句话；成功时 rail 的状态行已经说明一切。 */
private fun WakeOutcome.toNote(): String? = when (this) {
    is WakeOutcome.AgentOnline -> null
    is WakeOutcome.NoAnswer -> "Sent $packets wake packets — no answer from the Agent within ${formatWaited(waitedMillis)}"
    is WakeOutcome.Failed -> message
}

private fun formatWaited(millis: Long): String {
    val seconds = millis / 1000
    return if (seconds < 1) "<1 s" else "$seconds s"
}
```

`MainActivity` 注入真实发送器：`App(storage = storage, probe = AndroidTcpProbe(), wakeSender = UdpWakeOnLanSender())`。

- [ ] **Step 4: 运行测试确认通过**

Run: `JAVA_HOME=<jdk25> ./gradlew :composeApp:testDebugUnitTest :composeApp:desktopTest :composeApp:assembleDebug`
Expected: BUILD SUCCESSFUL；既有截图用例若因 rail 多渲染一行而变化，重出并复制到 `docs/plans/screenshots/`

- [ ] **Step 5: 提交**

```bash
git add mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/ui/powerrail mobile/composeApp/src/commonMain/kotlin/com/xukunz/wakeupmywall/app mobile/composeApp/src/androidMain docs/plans/screenshots
git commit -m "feat: wake the pc from the power rail and explain failures"
```

---

### Task 5: 验收（模拟器可验证的部分 + 真 PC 清单）

**Files:**
- Modify: `docs/superpowers/plans/2026-09-20-phase3-wol.md`（勾选 + 追加验收实录）
- Modify: `docs/plans/version-matrix.md`（补一行"WOL：JDK DatagramSocket，不新增依赖"）
- Modify: `docs/superpowers/plans/2026-09-19-roadmap.md`（Phase 3 段指向本计划的结果）

- [ ] **Step 1: 模拟器：包真的发出去了**

```bash
JAVA_HOME=<jdk25> ./gradlew :composeApp:installDebug
ADB=/home/xukunz/.local/toolchain/android-sdk/platform-tools/adb
"$ADB" -s emulator-5554 shell am force-stop com.xukunz.wakeupmywall
"$ADB" -s emulator-5554 logcat -c
"$ADB" -s emulator-5554 shell am start -n com.xukunz.wakeupmywall/.MainActivity && sleep 8
# 在 Settings 里把激活设备的 MAC 填上、Broadcast 设为 127.0.0.1、Save；回到 Dashboard 点电源环
"$ADB" -s emulator-5554 logcat -d -s WakeOnLan | tail -3
```
Expected: 出现 `sent 102 bytes x3 to 127.0.0.1:9`（或设备上配置的地址/端口）。

- [ ] **Step 2: 模拟器：无应答时给出具体原因**

用 `adb reverse tcp:9876 tcp:9876` + 宿主 stub Agent 的两种跑法各来一次：

```bash
# A) 不启动 stub（预期：超时 note）
# B) 启动 stub 后（预期：Agent 应答，rail 回到 ONLINE）
printf '{"hostname":"DESKTOP-ALPHA","agentVersion":"0.1.0","uptimeSeconds":42}' > /tmp/status.json
python3 - <<'PY' &
import http.server, json
class H(http.server.BaseHTTPRequestHandler):
    def do_GET(self):
        body = open('/tmp/status.json','rb').read()
        self.send_response(200); self.send_header('Content-Type','application/json')
        self.send_header('Content-Length', str(len(body))); self.end_headers(); self.wfile.write(body)
    def log_message(self, *a): pass
http.server.HTTPServer(('127.0.0.1', 9876), H).serve_forever()
PY
"$ADB" -s emulator-5554 reverse tcp:9876 tcp:9876
```
Expected: A 显示 `Sent 3 wake packets — no answer from the Agent within 60 s`（或注入的更短预算）；B 在轮询窗口内变 `PC Online`。两次各留一张截图进 `docs/plans/screenshots/phase3-*.png`。

- [ ] **Step 3: 真 PC 的 10/10 验收（需要用户执行）**

这一步**没有真 PC 就无法完成**，因此不在本计划里伪造结论。用户在有目标 PC 的局域网里按下面步骤做，结果填回本表：

1. Settings → Device Setup 填目标 PC 的 MAC、Broadcast IP（如 `192.168.1.255`）、WOL Port（9 或 7）；
2. 让 PC 进入真正的关机态（S5，不是"睡眠但网卡睡了"）；确认主板 BIOS/网卡已开 WOL；
3. 手机上点 Power Rail 的 Wake PC，记录结果；重复 10 次。

| 次数 | 结果（ONLINE / 超时 / 报错原文） | 备注（唤醒耗时、是否需再点一次） |
| --- | --- | --- |
| 1 | | |
| … | | |
| 10 | | |

Expected: 10/10 成功；若失败，把 `logcat -s WakeOnLan` 与界面上的 note 原文一起记下来。

- [ ] **Step 4: 更新文档并提交**

```bash
git add docs
git commit -m "docs: record the phase 3 acceptance run"
```

---

## 2. 验收标准（对照 roadmap Phase 3 段）

| roadmap 要求 | 本计划的落点 | 验证方式 |
| --- | --- | --- |
| `WakeOnLan` 接口 + Android 实现 | Task 2 | `androidUnitTest` 真 UDP 收包比对 |
| 魔法包字节构造（6×`0xFF` + 16 次 MAC） | Task 1 | `MagicPacketTest` 3 条逐字节断言 |
| UDP 广播 + 3 次重试 | Task 2、3 | 真 socket 收 3 个包；`WakeSequenceTest` 断言只发一次（sender 内部重试 3 次）且目标 = 广播地址:端口 |
| MAC / 广播地址校验 | Task 3 | 缺 MAC / 缺广播地址两条用例都不发包 |
| `WAKING → ONLINE/OFFLINE` 状态流转 | Task 3、4 | `WakeSequenceTest` 的事件序列 + `WakeFlowTest` 的 rail 断言 |
| 真机对目标 PC 连续 10 次开机成功 | Task 5 Step 3（**待用户执行**） | 记录表；模拟器只能验到"包发出去了 + 无应答有解释" |

## 3. 计划自检（writing-plans 要求的三项）

1. **Spec 覆盖**：§5 WOL（UDP 端口 9、发 3 次、随后每 2 秒轮询 `GET /api/v1/status`）→ Task 2（3 次、端口来自设备模型）+ Task 3（2 s 轮询）；§4 状态机 `WOL_READY → WAKING → ONLINE/OFFLINE` → Task 3 的 `PcEvent` 序列 + Task 4 的 rail 渲染；§6 模型字段（`broadcastAddress` / `wolPort` 已在 Phase 2 落地）→ Task 3 消费；roadmap 的完成标准 → §2 表。**刻意不做**：Agent 的睡眠/关机/重启（Phase 4）、`AGENT_UNAVAILABLE` 的判定细化（Phase 4/5 的连通性工作）。
2. **占位符扫描**：无 TBD；Task 5 Step 3 的表是**留给用户填真机结果**的记录表，不是待补代码。
3. **类型一致性**：`MagicPacket.encode` → `WakeOnLanSender.send(packet, target, port)` → `WakeSendResult.Sent/Failed` → `WakeOutcome.AgentOnline/NoAnswer/Failed` → `PcEvent.WakeRequested/AgentResponded/WakeTimedOut` → `PcStateMachine` → rail 的 `powerrail:state` / `powerrail:wake-note`。Task 1-4 的实现片段与测试逐处核对过命名。
