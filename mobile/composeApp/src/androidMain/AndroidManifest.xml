<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Test Connection 要真的发请求（Phase 2），Phase 4/5 的 Agent 与指标同样需要。 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      明文 HTTP 必须放行：Agent 就是局域网内的明文 HTTP 服务（spec §5：只监听局域网 + Bearer Token 鉴权），
      而 targetSdk ≥ 28 起 Android 默认**只对 localhost 放行明文**。手机连 PC 的局域网 IP 会被系统拦在
      HTTP 层，现象是"TCP 探得通，但 /status 请求失败"：
      `CLEARTEXT communication to 192.168.x.x not permitted by network security policy`。
      模拟器验收一直走 adb reverse + 127.0.0.1，恰好命中 localhost 豁免，所以直到真机才暴露。
    -->
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WakeUpMyWall">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|density|fontScale"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WakeUpMyWall"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
